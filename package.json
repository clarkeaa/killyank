{
	"name": "killyank",
	"displayName": "killyank",
	"description": "implement kill and yank",
	"version": "0.0.1",
	"publisher": "aaron",
	"engines": {
		"vscode": "^0.10.1"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.kill"
	],
	"main": "./out/src/extension",
	"contributes": {
		"commands": [{
			"command": "extension.kill",
			"title": "kill"
		}]
	},
	"scripts": {
		"vscode:prepublish": "node ./node_modules/vscode/bin/compile",
		"compile": "node ./node_modules/vscode/bin/compile -watch -p ./"
	},
	"devDependencies": {
		"typescript": "^1.6.2",
		"vscode": "0.10.x"
	}
}